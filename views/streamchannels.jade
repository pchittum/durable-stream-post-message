extends layout

block content
  header.bar.bar-nav
    h1.title= title
  ul.table-view
    li.table-view-cell.table-view-divider Available Streaming Channels
    each item in data
      li.table-view-cell: a.navigate-right(href='/streamchannel/#{item.getId()}') #{item.get('Description')}
